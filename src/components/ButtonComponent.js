import classes from "./components-styles/ButtonComponent.module.css";

const ButtonComponent = (props) => {
  return <button className={classes.btn}>English</button>;
};

export default ButtonComponent;
